'use babel';

import LanguageMinitestView from '../lib/language-minitest-view';

describe('LanguageMinitestView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
